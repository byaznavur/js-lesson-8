let obj = {
  firstName: "John",
  lastName: "Doe",
};

let keys = Object.keys(obj);

console.log(keys);

let values = Object.values(obj);

console.log(values);

// let entries = Object.entries(obj);

// console.log(entries);

// let arr = [
//   ["firstName", "John"],
//   ["lastName", "Doe"],
// ];

// let obj = Object.fromEntries(arr);

// console.log(obj);

// const obj = {
//   firstName: "John",
//   lastName: "Doe",
// };

// obj = 0;

// Object.freeze(obj);

// obj.firstName = "Abdulaziz";
// obj.lastName = "Programmer";

// obj.age = 24;

// delete obj.firstName;

// console.log(obj, Object.isFrozen(obj));

// Object.seal(obj);

// obj.firstName = "Abdulaziz";
// obj.lastName = "Programmer";

// obj.age = 24;

// delete obj.firstName;

// console.log(obj, Object.isSealed(obj));
